# تحديث عروض الأسعار — الحفظ السحابي واستيراد المستندات

## ١. خطوة مطلوبة قبل الاستخدام

افتح **Supabase Dashboard ← SQL Editor ← New query**، والصق محتوى الملف:

```
supabase/quotation-files-migration.sql
```

ثم اضغط Run. السكربت ينشئ:

- جدول `public.quotation_files` (بيانات الملفات المرفقة)
- مخزن الملفات `quotation-files` في Supabase Storage (حد ٢٥ ميجابايت للملف)
- صلاحيات RLS للجدول والمخزن

السكربت آمن ويمكن تشغيله أكثر من مرة. **بدونه سيفشل رفع الملفات.**

---

## ٢. ما تم إصلاحه

### الاستيراد من Excel لم يكن يُحفظ سحابيًا

السبب: Excel يخزّن التواريخ كأرقام تسلسلية (مثل `46001`). كان الكود يرسل هذا الرقم
كنص إلى عمود `date` في Postgres، فيرفض Supabase عملية الحفظ بالكامل، وكان الخطأ
يُسجَّل في الـ console فقط دون إشعار المستخدم. فتظهر البيانات على الشاشة ثم تختفي
عند تحديث الصفحة.

الإصلاح:

- دالة `toISODate()` تفهم: الرقم التسلسلي لإكسل، `01/09/2026`، `2026-09-01`،
  `1-Sep-2026`، `Sept 1, 2026`، والأرقام العربية `٠١/٠٩/٢٠٢٦`.
- قراءة الملف بـ `cellDates:true`.
- تنظيف قيمة المبلغ من الفواصل ورموز العملة.
- `saveStateOrWarn()` تعرض نص الخطأ الحقيقي عند فشل الحفظ بدل الفشل الصامت.

---

## ٣. الميزة الجديدة: استيراد مستند PDF / صورة / Word

زر **⭱ استيراد مستند** في شريط أدوات عروض الأسعار.

| الصيغة | طريقة القراءة |
|---|---|
| PDF | استخراج طبقة النص عبر pdf.js |
| JPG / PNG / WEBP | قراءة ضوئية OCR عبر Tesseract (عربي + إنجليزي) |
| DOCX | استخراج النص عبر mammoth.js |

### الحقول المستخرجة تلقائيًا

اسم الشركة · اسم المسؤول · الجوال · البريد الإلكتروني · المبلغ الإجمالي ·
رقم عرض السعر · رقم الفاتورة · رقم المشروع · التاريخ · نشاط الشركة ·
الموضوع · الحالة · وملخص من المستند في خانة التفاصيل.

إذا وُجد سطر "صالح لمدة ٣٠ يوم" يُحسب تاريخ المتابعة القادمة تلقائيًا.

### سلوك الاستيراد

- **ملف واحد** → تفتح نافذة المراجعة معبّأة تلقائيًا، تصحّح ما يلزم ثم تضغط حفظ.
  (نتائج OCR ليست مضمونة ١٠٠٪، لذلك المراجعة مقصودة.)
- **عدة ملفات دفعة واحدة** → تُضاف كلها تلقائيًا ثم تظهر رسالة بعدد الناجح والفاشل.

### حفظ الملف الأصلي

- يُرفع الملف إلى مخزن `quotation-files` في Supabase Storage.
- تُسجَّل بياناته في جدول `quotation_files` مرتبطة برقم عرض السعر.
- عند حذف عرض السعر تُحذف ملفاته من الجدول (ON DELETE CASCADE) ومن المخزن.
- الرفع يتم **بعد** نجاح حفظ عرض السعر في السحابة، منعًا لخطأ المفتاح الأجنبي.

### الملفات المرفقة داخل تفاصيل العرض

قسم جديد فيه لكل ملف: **عرض** · **طباعة** · **حذف**، وزر **إرفاق ملف** لإضافة
مرفقات لاحقة لعرض قائم.

الطباعة تنزّل الملف كـ blob ثم تطبعه مباشرة — هذه الطريقة الوحيدة الموثوقة لطباعة
ملف مستضاف على نطاق آخر. ملفات Word تُفتح في برنامج Word للطباعة.

---

## ٤. ملاحظة أمنية

صلاحيات المخزن والجدول مفتوحة للجميع، تمشيًا مع بقية جداول النظام الحالية. أي شخص
يملك رابط الملف يستطيع فتحه. إن كانت عروض الأسعار تحتوي بيانات حساسة، فعّل مصادقة
Supabase ثم استبدل في السكربت:

```sql
using (true)  -->  using (auth.uid() is not null)
```

---

## ٥. الملفات المعدّلة

| الملف | التغيير |
|---|---|
| `app.js` | إصلاح التواريخ، استخراج المستندات، رفع وطباعة المرفقات، ترجمات جديدة |
| `index.html` | زر استيراد مستند، شريط تنبيه الاستخراج، قسم المرفقات في التفاصيل |
| `style.css` | تنسيق شريط تنبيه الاستخراج |
| `supabase/schema.sql` | إضافة جدول `quotation_files` للتنصيب الجديد |
| `supabase/quotation-files-migration.sql` | **جديد** — الجدول والمخزن والصلاحيات |

---

## ٦. تحديث: سبب فشل استيراد ملف تقرير الزيارات اليومي

بعد فحص الملف `SALES_REP_DAILY_VISIT_REPORT` تبيّن سبب إضافي ومؤكّد للفشل:

عمود **Next Follow-up** لا يحتوي تواريخ، بل نصوصًا حرّة:

```
Waiting until next week
next week
this week
They will send email
pending the resumption of the projrct
ـــــــــــــــــــــــــــــــ
```

وعمود `next_followup` في قاعدة البيانات من نوع `date`. فكانت هذه النصوص تُرسل كما هي
إلى Postgres، فيرفض **العملية بأكملها** (خطأ 22007)، ولا يُحفظ أي صف من الصفوف العشرة.

### المعالجة

- النص غير القابل للتحويل إلى تاريخ لم يعد يُرسل إلى عمود التاريخ.
- ولا يُفقد أيضًا: يُحفظ في خانة **الملاحظات** بصيغة `المتابعة القادمة: next week`.
- علامات الشرطة الطويلة `ـــــــ` تُتجاهل لأنها تعني "لا يوجد".

### إصلاح ثانٍ في الملف نفسه

عمود **Follow-up Action / Next Step** لم يكن ضمن أسماء الأعمدة المعروفة في الكود
(كان يبحث عن `Follow-up Action` فقط)، فكان محتواه يُهمل بصمت. تمت إضافة الاسم الكامل.

### ملاحظة على هذا الملف

لا يوجد فيه عمود **Date**، لذلك تأخذ كل الصفوف التاريخ المختار في فلتر التاريخ أعلى
قائمة عروض الأسعار — وهو السلوك المطلوب في حالتك.

---

## ٧. تحديث: لماذا استمرت المشكلة رغم إصلاح التواريخ

بعد فحصٍ كامل لملفك عبر محاكاة منطق الكود، تبيّن أن كل القيم الآن تمرّ بسلامة (لا تواريخ
فاسدة، لا حقول ناقصة). ومع ذلك أخبرتني أن البيانات ما زالت "تظهر ثم تختفي بعد التحديث" —
وهذا بالضبط توقيع **فشل حفظ سحابي** لم تتنبّه له، لأن رسالة الخطأ كانت تظهر كـ"توست"
يختفي خلال ثانيتين فقط.

### ما تم إضافته الآن

- **شريط خطأ ثابت** أعلى قائمة عروض الأسعار لا يختفي تلقائيًا، ويعرض **النص الحرفي**
  لخطأ Supabase مع اسم الجدول الذي فشل فيه الحفظ (مثال: `[quotations] ...`).
- زر **نسخ نص الخطأ** لإرساله لي مباشرة.
- تفاصيل أدق للخطأ (كود الخطأ + الرسالة + التفاصيل + التلميح) بدل رسالة عامة.

### الخطوة التالية المطلوبة منك

جرّب استيراد نفس الملف مرة أخرى، وإن ظهر الشريط الأحمر انسخ نصّه بالكامل وأرسله لي —
عندها سأعرف السبب الدقيق (سواء كان عمودًا ناقصًا في قاعدة البيانات، أو صلاحية RLS،
أو غير ذلك) وأصلحه مباشرة بدل التخمين.

إن لم يظهر أي شريط أحمر إطلاقًا وبقيت المشكلة، فهذا يعني أن الحفظ نجح فعليًا، والمشكلة
عندئذٍ في **قراءة** البيانات بعد التحديث لا في كتابتها — وهو احتمال مختلف كليًا نحتاج
عندها لفحص شاشة "الإعدادات ← النسخ الاحتياطي" للتأكد أن الاتصال بـ Supabase يعمل من الأساس.
